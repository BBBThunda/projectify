body {
    padding-top: 20px;
    padding-bottom: 20px;
}

ul
{
    list-style-type: none;
}

.navbar {
    margin-bottom: 20px;
}

.navbar-brand {
    border: 2px;
    border-color: red;
}

input[text].project {
}

input[type="text"] {
  display: block;
  margin: 0;
  width: 100%;
  font-family: sans-serif;
  font-size: 18px;
  background: rgba(255,255,255,0.5);
  appearance: none;
  box-shadow: none;
  border-radius: none;
}

input[type="text"]:hover {
  outline: none;
}
